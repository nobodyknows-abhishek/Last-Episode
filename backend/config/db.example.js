// Example database config — DO NOT commit real credentials.
// Copy to `backend/config/db.js` and fill with real values
// or configure your app to read from environment variables.
module.exports = {
  host: 'localhost',
  port: 5432,
  user: 'dbuser',
  password: 'password',
  database: 'dbname',
  // ssl: { rejectUnauthorized: false }, // optional
};
